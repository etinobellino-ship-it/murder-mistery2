-- [[ NEGRO DI FIDUCIA HUB - COMBAT EDITION ]] --
-- Optimized for: Xeno Executor | Low-End PC
-- Features: Role-Based ESP, Insta-Kill Sheriff, Advanced Murderer Aura

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local RunService = game:GetService("RunService")
local RepStorage = game:GetService("ReplicatedStorage")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")

-- // SETTINGS
local Settings = {
    EspEnabled = false,
    KillAura = false,
    AuraRange = 20,
    KnifeThrowAura = false,
    SheriffInstaKill = false,
    WalkSpeed = 16,
    JumpPower = 50
}

-- // THEME
local Theme = {
    Main = Color3.fromRGB(10, 10, 15),
    Side = Color3.fromRGB(20, 20, 25),
    Accent = Color3.fromRGB(0, 170, 255),
    Text = Color3.fromRGB(255, 255, 255),
    Button = Color3.fromRGB(30, 30, 40),
    Close = Color3.fromRGB(255, 40, 40),
    MurderColor = Color3.fromRGB(255, 0, 0),
    SheriffColor = Color3.fromRGB(0, 170, 255)
}

-- // GUI SETUP
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local SideBar = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ContentFrame = Instance.new("Frame")
local TabContainer = Instance.new("Frame")
local CloseBtn = Instance.new("TextButton")
local DragonImg = Instance.new("ImageLabel")

pcall(function() ScreenGui.Parent = game:GetService("CoreGui") end)
if not ScreenGui.Parent then ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui") end
ScreenGui.Name = "NegroFiducia_Combat"

MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Theme.Main
MainFrame.Position = UDim2.new(0.3, 0, 0.25, 0)
MainFrame.Size = UDim2.new(0, 550, 0, 380)
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 12)
local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 2; MainStroke.Color = Theme.Accent

SideBar.Parent = MainFrame
SideBar.BackgroundColor3 = Theme.Side
SideBar.Size = UDim2.new(0, 140, 1, 0)
Instance.new("UICorner", SideBar).CornerRadius = UDim.new(0, 12)

DragonImg.Parent = SideBar
DragonImg.BackgroundTransparency = 1
DragonImg.Position = UDim2.new(0.1, 0, 0.05, 0)
DragonImg.Size = UDim2.new(0.8, 0, 0, 60)
DragonImg.Image = "rbxassetid://14532935813"

Title.Parent = SideBar
Title.Text = "NEGRO DI\nFIDUCIA HUB"
Title.TextColor3 = Theme.Accent
Title.TextSize = 16; Title.Font = Enum.Font.GothamBold
Title.Position = UDim2.new(0, 0, 0, 70); Title.Size = UDim2.new(1, 0, 0, 50)
Title.BackgroundTransparency = 1

CloseBtn.Parent = MainFrame
CloseBtn.Text = "X"; CloseBtn.TextColor3 = Color3.new(1,1,1)
CloseBtn.BackgroundColor3 = Theme.Close; CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -35, 0, 5); CloseBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)

ContentFrame.Parent = MainFrame
ContentFrame.Position = UDim2.new(0, 150, 0, 15)
ContentFrame.Size = UDim2.new(1, -160, 1, -30)
ContentFrame.BackgroundTransparency = 1

TabContainer.Parent = ContentFrame
TabContainer.Size = UDim2.new(1, 0, 1, 0)
TabContainer.BackgroundTransparency = 1

-- // UI HELPERS
local function CreateSection(name)
    local section = Instance.new("ScrollingFrame")
    section.Name = name
    section.Size = UDim2.new(1, 0, 1, 0)
    section.BackgroundTransparency = 1
    section.CanvasSize = UDim2.new(0, 0, 2, 0)
    section.ScrollBarThickness = 2
    section.Visible = false
    section.Parent = TabContainer
    Instance.new("UIListLayout", section).Padding = UDim.new(0, 8)
    return section
end

local function CreateToggle(text, default, parent, callback)
    local state = default
    local btn = Instance.new("TextButton")
    btn.Size = UDim2.new(1, -10, 0, 35)
    btn.BackgroundColor3 = state and Theme.Accent or Theme.Button
    btn.Text = text .. " : " .. (state and "ON" or "OFF")
    btn.TextColor3 = Theme.Text; btn.Font = Enum.Font.GothamMedium; btn.TextSize = 13; btn.Parent = parent
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
    btn.MouseButton1Click:Connect(function()
        state = not state
        btn.Text = text .. " : " .. (state and "ON" or "OFF")
        btn.BackgroundColor3 = state and Theme.Accent or Theme.Button
        callback(state)
    end)
end

local function CreateSlider(text, min, max, default, parent, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(1, -10, 0, 45)
    frame.BackgroundColor3 = Theme.Button
    frame.Parent = parent
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)

    local lbl = Instance.new("TextLabel")
    lbl.Text = text .. ": " .. default
    lbl.Size = UDim2.new(1, 0, 0, 20)
    lbl.TextColor3 = Theme.Text; lbl.BackgroundTransparency = 1; lbl.Parent = frame

    local slider = Instance.new("TextButton")
    slider.Size = UDim2.new(0, 0, 0, 10)
    slider.Position = UDim2.new(0, 0, 0.7, 0)
    slider.BackgroundColor3 = Theme.Accent; slider.Text = ""; slider.Parent = frame

    -- Simple click-to-increase for low-end PC (avoids laggy dragging)
    slider.MouseButton1Click:Connect(function()
        local newVal = default + 5
        if newVal > max then newVal = min end
        default = newVal
        lbl.Text = text .. ": " .. default
        callback(default)
    end)
end

-- // TABS SETUP
local MainTab = CreateSection("Main")
local CombatTab = CreateSection("Combat")
local VisualTab = CreateSection("Visuals")

MainTab.Visible = true

local tabs = {{"Main", MainTab}, {"Combat", CombatTab}, {"Visuals", VisualTab}}
for i, tabData in ipairs(tabs) do
    local tBtn = Instance.new("TextButton")
    tBtn.Size = UDim2.new(0, 120, 0, 35); tBtn.Position = UDim2.new(0, 10, 0, 130 + (i*40))
    tBtn.BackgroundColor3 = Theme.Button; tBtn.Text = tabData[1]; tBtn.TextColor3 = Theme.Text; tBtn.Parent = SideBar
    Instance.new("UICorner", tBtn).CornerRadius = UDim.new(0, 6)
    tBtn.MouseButton1Click:Connect(function()
        for _, v in pairs(TabContainer:GetChildren()) do v.Visible = false end
        tabData[2].Visible = true
    end)
end

-- // FEATURES IMPLEMENTATION

-- [ MAIN ]
CreateToggle("Fly", false, MainTab, function(v) Settings.FlyEnabled = v end)
CreateToggle("Speed Boost", false, MainTab, function(v) Settings.WalkSpeed = v and 60 or 16 end)

-- [ VISUALS ] (Sceriffo Blu / Murder Rosso)
CreateToggle("Role ESP", false, VisualTab, function(v) Settings.EspEnabled = v end)

-- [ COMBAT ] - Divisione Sceriffo e Murderer
local SheriffLabel = Instance.new("TextLabel", CombatTab)
SheriffLabel.Text = "--- SHERIFF SECTION ---"; SheriffLabel.Size = UDim2.new(1,0,0,20); SheriffLabel.TextColor3 = Theme.SheriffColor; SheriffLabel.BackgroundTransparency = 1

CreateToggle("Insta-Kill Murderer (Click)", false, CombatTab, function(v) Settings.SheriffInstaKill = v end)

local MurderLabel = Instance.new("TextLabel", CombatTab)
MurderLabel.Text = "--- MURDERER SECTION ---"; MurderLabel.Size = UDim2.new(1,0,0,20); MurderLabel.TextColor3 = Theme.MurderColor; MurderLabel.BackgroundTransparency = 1; MurderLabel.Position = UDim2.new(0,0,0,50)

CreateToggle("Kill Aura", false, CombatTab, function(v) Settings.KillAura = v end)
CreateToggle("Knife Throw Aura (Auto)", false, CombatTab, function(v) Settings.KnifeThrowAura = v end)
CreateSlider("Aura Range", 10, 100, 20, CombatTab, function(v) Settings.AuraRange = v end)

-- // LOGIC ENGINE

-- Raycast check for visibility (Muri)
local function IsVisible(target)
    local origin = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") and LocalPlayer.Character.HumanoidRootPart.Position or Vector3.new()
    local targetPos = target.Character and target.Character:FindFirstChild("HumanoidRootPart") and target.Character.HumanoidRootPart.Position or Vector3.new()
    local direction = (targetPos - origin).Unit * (targetPos - origin).Magnitude
    local raycastParams = RaycastParams.new()
    raycastParams.FilterDescendantsInstances = {LocalPlayer.Character}
    raycastParams.FilterType = Enum.RaycastFilterType.Blacklist
    local result = workspace:Raycast(origin, direction, raycastParams)
    return result == nil or result.Instance:IsDescendantOf(target.Character)
end

-- Sheriff Insta-Kill Logic (Mouse Click)
UIS.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.UserInputType == Enum.UserInputType.MouseButton1 and Settings.SheriffInstaKill then
        local mouseTarget = LocalPlayer:GetMouse().Target
        if mouseTarget and mouseTarget.Parent:FindFirstChild("Humanoid") then
            local targetPlayer = Players:GetPlayerFromCharacter(mouseTarget.Parent)
            if targetPlayer and targetPlayer:GetAttribute("Role") == "Murderer" and IsVisible(targetPlayer) then
                -- Remote fire logic for gun kill
                for _, v in pairs(RepStorage:GetDescendants()) do
                    if v:IsA("RemoteEvent") and v.Name:lower():find("shoot") then v:FireServer(targetPlayer) end
                end
            end
        end
    end
end)

RunService.Heartbeat:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        LocalPlayer.Character.Humanoid.WalkSpeed = Settings.WalkSpeed
    end

    -- Murderer Advanced Aura Logic
    if Settings.KillAura or Settings.KnifeThrowAura then
        local char = LocalPlayer.Character
        if char and (char:FindFirstChild("Knife") or LocalPlayer:GetAttribute("Role") == "Murderer") then
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (char.HumanoidRootPart.Position - p.Character.HumanoidRootPart.Position).Magnitude
                    if dist <= Settings.AuraRange and IsVisible(p) then
                        if Settings.KillAura then -- Melee Kill
                            for _, v in pairs(RepStorage:GetDescendants()) do
                                if v:IsA("RemoteEvent") and v.Name:lower():find("attack") then v:FireServer(p) end
                            end
                        elseif Settings.KnifeThrowAura then -- Auto Throw
                            for _, v in pairs(RepStorage:GetDescendants()) do
                                if v:IsA("RemoteEvent") and v.Name:lower():find("throw") then v:FireServer(p) end
                            end
                        end
                    end
                end
            end
        end
    end
end)

RunService.RenderStepped:Connect(function()
    if Settings.EspEnabled then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local hl = p.Character:FindFirstChild("XenoESP") or Instance.new("Highlight", p.Character)
                hl.Name = "XenoESP"
                -- Colore basato sul ruolo
                if p:GetAttribute("Role") == "Murderer" then hl.FillColor = Theme.MurderColor else hl.FillColor = Theme.SheriffColor end
                hl.OutlineColor = Color3.new(1,1,1)
            end
        end
    end
end)

-- Close & Drag logic (Standard)
CloseBtn.MouseButton1Click:Connect(function() ScreenGui:Destroy() end)

local dragging, dragStart, startPos
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true; dragStart = input.Position; startPos = MainFrame.Position end
end)
UIS.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement and dragging then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
UIS.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end)

print("Negro di Fiducia Hub - Combat Edition Loaded!")
